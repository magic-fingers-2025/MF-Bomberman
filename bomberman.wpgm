import wollok.game.*
import personajes.*
import fondo.*
import bomba.*
import menu.*
import interfaz.*
import coordenadasBloqueadas.*
import controlador.*

program bombermanGame {

  const ancho = 22
  const alto = 20
  // límites del juego
  game.width(ancho)
  game.height(alto)
  game.cellSize(48) // tamaño celda 48

  // arrancar el controlador
  controlador.iniciarJuego()

  game.start()
}